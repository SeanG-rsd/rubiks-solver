export type Rectangle = {
    x: number;
    y: number;
    width: number;
    height: number;
};

export type Color = {
    red: number;
    green: number;
    blue: number;
    alpha: number;
}

export type Range = {
    color: string;
    min: Color;
    max: Color;
}